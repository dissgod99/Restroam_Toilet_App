import React from "react";
import { ScrollView, StyleSheet } from "react-native";

const OutloggedScreen = () => {

    return(
        <ScrollView>

        </ScrollView>
    )
}

const styles = StyleSheet.create({

});

export default OutloggedScreen;